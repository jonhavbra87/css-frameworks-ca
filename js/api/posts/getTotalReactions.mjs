// export async function getTotalReactions(posts) {
//     if (!post || !post.reactions) {
//         console.log("postData or postData.reactions is undefined");
//         return 0; // Returner 0 eller en annen passende verdi
//       }
//       return posts.reactions.reduce((total, reaction) => total + reaction.count, 0);
//     }
  